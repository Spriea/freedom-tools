document.querySelectorAll(".contact-copy").forEach((button)=>button.addEventListener("click",async()=>{const original=button.textContent;try{if(navigator.clipboard&&window.isSecureContext){await navigator.clipboard.writeText(button.dataset.copy)}else{const input=document.createElement("textarea");input.value=button.dataset.copy;input.style.position="fixed";input.style.opacity="0";document.body.appendChild(input);input.select();if(!document.execCommand("copy"))throw new Error("copy failed");input.remove()}button.textContent=button.dataset.copied||"Copied"}catch(error){button.textContent=button.dataset.failed||"Copy failed"}window.setTimeout(()=>{button.textContent=original},1500)}));
